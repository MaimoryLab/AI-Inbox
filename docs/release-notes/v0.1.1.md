# AI-Index v0.1.1

AI-Index v0.1.1 switches the public desktop download path from zipped command-line binaries to platform installers.

AI-Index is a local-first workspace for reviewing follow-up cards extracted from Codex and Claude Code sessions. The installers bundle the AI-Index runtime and web UI assets, so desktop users do not need to install Node.js.

## Downloads

- macOS Apple Silicon: `ai-index-macos-arm64.dmg`
- Windows x64: `ai-index-windows-x64.msi`

## Start

- macOS: open the DMG, then open `AI-Index.app`.
- Windows: run the MSI, then open **AI-Index** from the Start menu.

AI-Index opens a local browser workspace at `127.0.0.1:3111` by default. Configure source paths and your OpenAI-compatible LLM key in Settings, then click **Organize**.

## Notes

- The primary CLI command is `ai-index start`. The `open` command remains as a hidden compatibility alias.
- Config and data are stored under `~/.ai-index` by default, or under `AI_INDEX_HOME` if you override it.
- The installers are currently unsigned, so macOS Gatekeeper or Windows Defender may ask for confirmation on first launch.
- CI builds and verifies the installers on macOS and Windows with Node 22.x.

---

# AI-Index v0.1.1 中文说明

AI-Index v0.1.1 将桌面下载方式从 zip 命令行二进制切换为平台安装器。

AI-Index 是一个本地优先的工作台，用于复查从 Codex 和 Claude Code 会话中整理出的后续事项卡片。安装器已包含 AI-Index 运行时和 Web UI 静态资源，桌面用户不需要安装 Node.js。

## 下载

- macOS Apple Silicon：`ai-index-macos-arm64.dmg`
- Windows x64：`ai-index-windows-x64.msi`

## 启动

- macOS：打开 DMG，然后打开 `AI-Index.app`。
- Windows：运行 MSI，然后从开始菜单打开 **AI-Index**。

AI-Index 默认会在 `127.0.0.1:3111` 打开本地浏览器工作台。请在 Settings 中配置 source paths 和 OpenAI-compatible LLM key，然后点击 **Organize**。

## 说明

- 主要 CLI 命令是 `ai-index start`。`open` 命令仍作为隐藏兼容别名保留。
- 配置和数据默认保存在 `~/.ai-index`，也可以通过 `AI_INDEX_HOME` 覆盖。
- 当前安装器尚未签名，macOS Gatekeeper 或 Windows Defender 可能会在首次运行时要求确认。
- CI 会在 Node 22.x 上构建并验证 macOS 和 Windows 安装器。
